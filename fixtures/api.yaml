openapi: 3.0.0
info:
  title: Sample API
  version: 1.0.0
paths:
  /users/login:
    post:
      summary: User Login
  /products/{id}:
    get:
      summary: Get Product Details
  /old/legacy-endpoint:
    get:
      summary: This one is deprecated